import React from 'react';

const Header = () => (
  <div style={{ textAlign: 'center' }}>
    <h1>
      Broadcast Bundles
    </h1>
    Choose a bundle or a single plan!
  </div>
);

export default Header;
